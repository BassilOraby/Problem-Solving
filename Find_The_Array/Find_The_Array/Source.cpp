#include<iostream>
#include<vector>
using namespace std;

int main() {
	int t, s;
	vector<int> v;
	scanf_s("%d", &t);
	while (t--)
	{
		scanf_s("%d", &s);
		if (s == 1)
		{
			v.push_back(s);
			continue;
		}

	}
	return 0;
}